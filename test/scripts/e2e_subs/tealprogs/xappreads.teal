#pragma version 9
// We use version 9 so we can test --access which needs at least sharedResourcesVersion

// Fetch app idx we want to read global state from
int 1  // ForeignApps index
// non-utf8 key
byte 0xfefeffef0000112233

// Get value
app_global_get_ex

// Should exist
assert

// Value should be "bar"
byte "bar"
==
assert

int 1

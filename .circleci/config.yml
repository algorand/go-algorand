version: '2.1'
orbs:
  slack: circleci/slack@4.1
jobs:
  notify:
    docker:
      - image: 'cimg/base:stable'
    steps:
workflows:
  send-notification:
    jobs:
      - notify:
          filters:
            branches:
              only:
                - /pull\/[0-9]+/



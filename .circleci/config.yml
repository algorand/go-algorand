version: '2.1'
orbs:
  slack: circleci/slack@4.1
jobs:
  notify:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - run:
          name: "echo environment variables from context"
          command: echo $testabc && echo $TEST
workflows:
  send-notification:
    jobs:
      - notify:
          context:
            - "slack-secrets"
            - test

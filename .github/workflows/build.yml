name: "Build workflow"
on:
  pull_request:
jobs:
  build-test-windows:
    runs-on: windows-2022
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - name: Install msys2
        uses: msys2/setup-msys2@v2
        with:
          update: true
          path-type: inherit
      # - name: Check out code
      #   uses: actions/checkout@v2
      #   with:
      #     fetch-depth: 0
      # - uses: actions/setup-go@v2
      #   with:
      #     go-version: '1.14.7'
      # - uses: actions/setup-python@v2
      #   with:
      #     python-version: '3.7.9'
      # - name: Build Test
      #   run: |
      #     export ALGORAND_DEADLOCK=enable
      #     export SKIP_GO_INSTALLATION=True
      #     scripts/travis/build_test.sh
      - name: test upload to s3
        run: |
          touch jacks-test.txt
          # git ls-files -o | grep -v crypto/libs | grep -v crypto/copies | grep -v crypto/libsodium-fork | grep -v crypto/lib/ | grep -v ^gen/ | grep -v swagger.json.validated | tr "\n" ":"
      - name: Upload to S3
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          aws s3 cp \
          #--recursive \
          #--acl public-read \
          --region us-east-1 \
          # $(git ls-files -o | grep -v crypto/libs | grep -v crypto/copies | grep -v crypto/libsodium-fork | grep -v crypto/lib/ | grep -v ^gen/ | grep -v swagger.json.validated | tr "\n" ":") \
          jacks-test.txt \
          s3://${{ secrets.ARTIFACTS_BUCKET }} --dry-run
